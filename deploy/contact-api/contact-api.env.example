# copy to /etc/contact-api.env  (chmod 600, owned by root) — never commit the real one
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=ops@corazon-tech.com
SMTP_PASS=change-me
MAIL_TO=ops@corazon-tech.com
MAIL_FROM=ops@corazon-tech.com
ALLOWED_ORIGIN=https://corazon-tech.com
